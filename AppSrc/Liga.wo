// C:\DataFlex Projects\FutbolWebApp\AppSrc\Liga.wo
// Liga
//

Use cWebView.pkg
Use cWebList.pkg
Use cWebMenuGroup.pkg
Use cWebMenuItem.pkg
Use cWebColumnButton.pkg
Use cWebColumn.pkg
Use ZoomLiga.wo

Use cligaDataDictionary.dd

Object oLiga is a cWebView
    Set psCaption to "Liga"
    Set peWebViewStyle to wvsDrilldown
    Set peViewType to vtSelect
    Set piColumnCount to 12
    Set pbShowCaption to False
    Set piMaxWidth to 1024

    Object oliga_DD is a cligaDataDictionary
    End_Object 

    Set Main_DD to oliga_DD
    Set Server  to oliga_DD


    Object oList is a cWebList
        Set piColumnSpan to 0
        Set psCSSClass to "MobileList"
        Set pbServerOnRowClick to True
        Set pbFillHeight to True
        Set pbShowHeader to False
        Set piSortColumn to 0

        // Registramos la ruta hacia la vista Zoom de la Liga
        WebRegisterPath ntNavigateForward oZoomLiga

        Object oligaID is a cWebColumn
            Entry_Item liga.ID
            Set psCaption to "ID"
            Set piWidth to 70
        End_Object 

        Object oligaNombre is a cWebColumn
            Entry_Item liga.Nombre
            Set psCaption to "Liga"
            Set piWidth to 450
        End_Object 

        Object oligaPais is a cWebColumn
            Entry_Item liga.Pais
            Set psCaption to "País"
            Set piWidth to 250
        End_Object 

        // Columna calculada para mostrar cupos en formato compacto (ej: 3 / 20)
        Object oligaCuposCalculados is a cWebColumn
            Set psCaption to "Equipos"
            Set piWidth to 120
            Set peAlign to alignCenter

            Procedure OnSetCalculatedValue String ByRef sValue
                Move (Trim(String(liga.Equipos)) + " / " + Trim(String(liga.EquiposMax))) to sValue
            End_Procedure
        End_Object

        // Ocultamos las columnas individuales para que no descuadren la fila móvil,
        // pero se quedan declaradas con Entry_Item para mantener el buffer del DD activo.
        Object oligaEquipos is a cWebColumn
            Entry_Item liga.Equipos
            Set pbRender to False
        End_Object 

        Object oligaEquiposMax is a cWebColumn
            Entry_Item liga.EquiposMax
            Set pbRender to False
        End_Object 

        Object oDetailButton is a cWebColumnButton
            Set psCaption to "btn"
            Set piWidth to 45
            Set pbFixedWidth to True
            Set pbResizable to False
            Set piListRowSpan to 1
            Set psBtnCssClass to "WebButtonIcon WebIcon_Info"
            Set peAlign to alignRight

            WebRegisterPath ntNavigateForward oZoomLiga

            Procedure OnClick
                Send NavigatePath
            End_Procedure

            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bReadOnly
            End_Procedure

        End_Object 

        Procedure OnRowClick String sRowId
            tWebNavigateData NavigateData
            Get GetNavigateData to NavigateData

            Case Begin
                Case (NavigateData.eNavigateType = nfFromParent)
                    // Se habilita la navegación real hacia oZoomLiga
                    Send NavigateForward of oZoomLiga Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromChild)
                    Send NavigateClose Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromMain)
                    Send NavigateClose Self
                    Case Break

                Case Else // must be nfUndefined
                    // Se habilita la navegación real hacia oZoomLiga
                    Send NavigateForward of oZoomLiga Self

            Case End
        End_Procedure

        Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
            Move True to NavigateData.bReadOnly
        End_Procedure

    End_Object 

    Object oActionGroup is a cWebMenuGroup

        Object oSearch is a cWebMenuItem
            Set psCSSClass to "WebPromptMenuItem"
            Set psCaption to "Search"

            Procedure OnClick
                Send Search of oList
            End_Procedure

        End_Object 

        Object oNewButton is a cWebMenuItem
            Set psCSSClass to "WebClearMenuItem"
            Set psCaption to "New"

            WebRegisterPath ntNavigateForward oZoomLiga

            Procedure OnClick
                Send NavigatePath
            End_Procedure

            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bNewRecord
            End_Procedure

        End_Object 
    End_Object 

    Procedure OnNavigateForward tWebNavigateData NavigateData Integer hoInvokingView Integer hoInvokingObject
        WebSet pbRender of oNewButton to True
        WebSet pbRender of oDetailButton to True

        Case Begin
            Case (NavigateData.eNavigateType = nfFromParent)
                Case Break

            Case (NavigateData.eNavigateType = nfFromChild)
                Case Break

            Case (NavigateData.eNavigateType = nfFromMain)
                WebSet pbRender of oDetailButton to False
                Case Break

            Case Else 
        Case End

    End_Procedure

End_Object